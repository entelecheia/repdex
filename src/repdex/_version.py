"""Version information for the Repdex package.

This module contains the version string for the package. It follows semantic versioning
(https://semver.org/) with the format MAJOR.MINOR.PATCH.

Note:
    This is a private module and should not be imported directly by users.
    Use `repdex.get_version()` instead.

Example:
    >>> from repdex._version import __version__
    >>> print(__version__)
    '0.1.0'
"""

__version__ = "0.0.0"
